name: Deploy Egyptian Life Simulator

on:
  push:
    branches: [main]
    paths:
      - 'egypt-life-sim-v2/**'
      - '.github/workflows/deploy-egypt-life-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Validate game files
        run: |
          test -s egypt-life-sim-v2/index.html
          test -s egypt-life-sim-v2/game.js
          node --check egypt-life-sim-v2/game.js
          grep -q 'game.js' egypt-life-sim-v2/index.html
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
